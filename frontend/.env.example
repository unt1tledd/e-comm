# Для dev (npm run dev) можно не задавать — прокси в vite.config разведёт запросы.
# Для preview/prod (npm run build && npm run preview) задай явно:
# VITE_CART_URL=http://localhost:8080
# VITE_LOMS_URL=http://localhost:8081
